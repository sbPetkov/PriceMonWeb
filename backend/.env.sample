# Environment Variables Template
# Copy this file to .env.dev (development) or .env.prod (production) and fill in your values

# Django Core Settings
SECRET_KEY=your-secret-key-here-generate-new-one
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Database Configuration
# For SQLite (development):
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# For PostgreSQL (production):
# DATABASE_ENGINE=django.db.backends.postgresql
# POSTGRES_DB=pricemon
# POSTGRES_USER=pricemon_user
# POSTGRES_PASSWORD=your-secure-password
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432

# CORS Settings
# Development: set to True
# Production: set to False and configure CORS_ALLOWED_ORIGINS
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=https://price-mon.com,https://www.price-mon.com

# Frontend URL
FRONTEND_URL=http://localhost:5173
